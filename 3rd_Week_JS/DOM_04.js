//using js you can manipulate the DOM (basically the HTML structure)
// and you can add, remove, modify the elements in the DOM
// and you can also add event listeners to the elements in the DOM  

//we can run js code in the browser and in the node (outside the browser)

