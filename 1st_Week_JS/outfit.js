//1st challenge (datatype)
//server call, rainy, cloudy , sunny 

let weather = undefined;

if(weather === "rainy"){
    console.log("It's raining, umbrrella le jaana bhai")
    //
}else if(weather === "cloudy") {
    console.log("jacket le he lena")
    //
}else{
    console.log("It is sunny")
}

//else if mei condition check kr skte hai but else mei
//condition ni check kr sakte hai 

//when we assign a single equal to, example = it means we assign a value

//console.log(weather === "rainy") //false
// '===' puchta hai question whether it's true or not
//'==' means 
// == (loose equality) compares:
// Values, but with type conversion (coercion) if types are different.
// What it actually compares:
// If both values are same type, it compares directly.

// If types are different, JS tries to convert one or both values to the same type and then compares.
